name: 👕
on:
  push:
    branches: [main]
  pull_request:

jobs:
  dprint:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dprint/check@v2.5
        with:
          dprint-version: '0.57.4' # selfup { "extract": "\\d[^']+", "replacer": ["dprint", "--version"], "nth": 2 }
